#!/user/bin/env node
import { program } from 'commander';
import figlet from 'figlet';
import mainPrompt from './prompts/main.prompt.js';
const banner = figlet.textSync('Plural Ripper', {
    font: 'Standard',
    horizontalLayout: 'default',
    verticalLayout: 'default',
    width: 80,
    whitespaceBreak: true,
});
console.log(banner);
program.version('0.1.0');
program
    .option('-j, --json <json>', 'JSON file path generated by instruction from https://github.com/sayeed205/plural-ripper')
    .option('-o, --output <output>', 'Output directory path')
    .description('Download courses from https://pluralsight.com/')
    .action(options => {
    console.log(options);
});
if (process.argv.length > 2) {
    program.parse(process.argv);
}
else {
    mainPrompt();
}
//# sourceMappingURL=main.js.map